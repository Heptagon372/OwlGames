# DECISIONS

`OWLGAMES_SPEC.md`에 명시되지 않았거나, 구현하면서 명세와 다르게 간 부분을 기록한다.
(§ 번호는 설계 문서 기준. 새 결정은 아래에 계속 추가할 것)

## 1. 스택 / 프로젝트

| 결정 | 이유 |
|---|---|
| Next.js 15.5 + React 19 + Tailwind CSS **v4** (CSS-first `@theme`) | 명세의 스택 지정을 따르되 Tailwind는 현행 버전. 토큰을 `app/globals.css` 한 곳에 모을 수 있다 |
| 로비·랭킹·티켓·내기록을 route group `app/(player)/`로 묶음 | URL은 §12 그대로(`/lobby` 등)이고, 공통 셸(헤더·하단 탭바)을 한 곳에서 관리 |
| **데모 모드** (`lib/env.ts`의 `isDemo`) | Supabase 환경변수가 없으면 가짜 데이터로 전 화면이 뜬다. 디자인 리뷰·발표·CI 빌드에 DB가 필요 없다 |
| 아이콘은 `lucide-react`, 그 외 그래픽은 SVG/CSS 직접 작성 | 이미지 에셋 없이 시작하라는 §0 지침. 부엉이 마크·랭크 뱃지·뽑기 기계 모두 코드로 그린다 |
| 테스트는 vitest (`tests/`) | 레벨 곡선(Lv100 = 27,720P)·랭크 경계·데이터 파일 형식을 CI에서 검증 |

## 2. 게임 (§7)

| 결정 | 이유 |
|---|---|
| **`game_limits.typer.min_sec` = 55 → 10** | 방화벽 100%면 60초 전에 조기 종료되는데, 55초 하한이면 정상 플레이가 `rejected` 된다 |
| 콤보 구간: 연속 3회 ×1.2, 6회 ×1.5, 10회 ×2.0 | §7.1은 배율만 정하고 조건이 없었다 |
| 타이퍼 난이도 분포: 0~20초 쉬움 위주, 20~40초 보통 위주, 40초~ 어려움 위주 | "시간이 지날수록 길어진다"의 구체화 |
| 피싱 헌터만 카드 UI를 **DOM**으로 구현 (타이머는 rAF) | 한글 본문 줄바꿈·스크린리더·스와이프 제스처가 캔버스보다 안전하다. 엔진 라이브러리 금지 원칙은 지킴 |
| 피싱 카드 70장 (정상 28 : 피싱 42 = 4:6), 한 판 안에서 셔플·중복 없음. 카드를 다 쓰면 조기 종료 | §7.3 최소 60장 충족 |
| 비행: 픽셀→미터 환산 18px = 1m, 최대 속도 340px/s, 시작 전 대기 시간도 180초 상한에 포함 | 세션 상한(`max_sec` 185) 안에서 끝나게 하기 위함 |
| 게임 시작 전 3·2·1 카운트다운 | 세션은 카운트다운 전에 발급된다(제출 시 경과시간 = 카운트다운 포함) |

## 3. DB / RPC (§10)

| 결정 | 이유 |
|---|---|
| **`board_events` 테이블 추가** (kind, masked_name, rank_idx, place, prize_name) | 전광판은 비로그인 공개 화면이라 `draws`·`rank_events`를 직접 구독할 수 없다. 마스킹된 공개 이벤트만 Realtime으로 흘린다 (꽝은 송출하지 않음) |
| Realtime 퍼블리케이션에 `board_events`, `profiles` 등록 | `profiles`는 `/pending`에서 본인 승인 이벤트를 받기 위해 (RLS로 본인 행만) |
| 뷰 `leaderboard`, `game_bests` (이름 마스킹, 인증 유저만) | §10.4의 마스킹 요구. 뷰는 소유자 권한으로 돌아 `profiles` RLS를 우회한다 |
| `expire_stale()`을 RPC **끝**에서 호출 | 시작에서 부르면 다른 유저의 만료 행 잠금을 트랜잭션 끝까지 쥐고 있어 교착이 난다. 각 RPC가 만료 여부를 직접 확인하므로 동작은 동일 |
| `draws.staff_id` nullable + `on delete set null`, `draws.user_id`/`ticket_id` cascade, `tickets.redeem_code_id` set null | 부원·유저 계정을 지울 때 FK 때문에 실패하지 않도록 (§10.3 `admin_delete_user`) |
| `tickets`에 `unique(user_id, earned_rank_idx)` | "랭크 상승 1회당 1장 = 최대 16장"을 구조적으로 보장 |
| `issue_redeem_code`에 유저별 advisory lock | 두 기기에서 동시에 발급을 눌러도 코드가 하나만 남게 |
| `admin_set_role`, `admin_set_force_open`, `admin_set_stock`, `board_stats()` 추가 | 관리자 화면(§4 `/admin`)에 필요한데 §10.3 목록에는 없었다 |
| `app_config`에 `student_id_pattern`, `redeem_code_ttl_min` 추가 | §11의 학번 정규식·§8.1의 10분 만료를 설정으로 뺐다 |
| 추첨 난수는 `extensions.gen_random_bytes(6)` → [0,100) 실수 | `search_path`를 고정했기 때문에 스키마를 명시해서 호출 |

## 4. 인증 (§11)

| 결정 | 이유 |
|---|---|
| `SUPABASE_SERVICE_ROLE_KEY`가 있으면 `auth.admin.createUser(email_confirm: true)`로 가입 | `@owlgames.local`은 실제 수신이 안 되는 주소라 확인 메일 설정에 의존하지 않게. 키가 없으면 `signUp`으로 폴백(대시보드에서 Confirm email 끄기 필요) |
| 학번 중복은 ①service_role로 `profiles` 선조회 ②GoTrue 오류 문구 매칭 두 단계로 안내 | 트리거의 unique 위반이 클라이언트에는 "Database error saving new user"로 도착한다 |
| 첫 관리자는 SQL로 직접 지정 | 가입 트리거가 메타데이터 없는 대시보드 계정을 거부하므로 |
| 미들웨어에서 매 요청 `profiles(role, verified)` 조회 | 부스 행사 규모(동시 100명)에서는 캐시보다 단순함이 이득 |

## 5. 디자인 (§13)

| 결정 | 이유 |
|---|---|
| 랭크 뱃지는 육각형 + 그라데이션 + 영문 2글자, 신화=무지개(회전 hue), 초월자=백색 발광, 챌린저=앰버 발광+파티클 | §13의 색 지시를 시각 규칙으로 구체화 |
| 배경은 `body` 고정 레이어(`.night-sky` 색 블룸·격자 + `.scanlines` 필름 그레인, §5-10) | 페이지마다 배경을 칠하지 않아도 톤이 유지된다 |
| 모바일 하단 탭바(로비·랭킹·티켓·내기록), 부스·전광판·관리자는 가로 레이아웃 | §0 모바일 우선 / 키오스크·전광판은 태블릿·TV |
| 숫자·코드는 `.num`(JetBrains Mono, tabular-nums) | 카운트다운·점수가 흔들리지 않게 |

## 5-1. 아울러닝 (OWL RUNNING) — `OWLRUNNING_GDD.md` 구현

| 결정 | 이유 |
|---|---|
| `flight` 게임을 v2(아울러닝)로 **전면 교체**. game_id는 `flight` 그대로, 표시명만 "아울러닝" | 기획서 §0 지시 — 마이그레이션·랭킹 기록이 그대로 이어진다 |
| `app_config.game_k.flight` 3 → **20** (마이그레이션 `20260924000000_owlrunning.sql`) | v2 원점수 스케일이 훨씬 커졌다 (§11.2) |
| 캔버스는 **논리 해상도 960×540 고정**, CSS로 레터박스 스케일 | 기기마다 같은 난이도를 보장 (§1) |
| 입력: 플레이 영역 **아무 곳이나** 누르면 상승 (기획서는 "왼쪽 절반") | 색상 버튼이 DOM 오버레이라 충돌이 없고, 부스에서 처음 잡는 사람에게 더 관대하다 |
| `narrow`(S 전용 통로)는 **위아래 70px 띠만 벽**이고 나머지 높이는 열려 있다 | 전체를 막으면 M·L은 통과 자체가 불가능해진다. 띠 구조라야 "S는 지름길, M·L은 우회"가 성립 (§6) |
| P0 청크는 **바닥 차선(y ≥ 500)을 비워 둔다** — 검증기가 강제 | §16-3 "아무 입력도 안 하면 죽지 않는다"를 구조적으로 보장 |
| 청크 검증을 vitest로 구현 (`npm run verify:chunks`) | 엔진과 **같은 물리·충돌 함수**를 써야 검증이 의미가 있다. 별도 스크립트로 두면 상수가 갈라진다 |
| `bug` 위상은 시계가 아니라 **청크 진행도**로 계산 | 검증기와 런타임이 정확히 같은 궤적을 보게 하려고 |
| HUD 점수에서는 `energy_left × 2`를 빼고 표시 | 날갯짓할 때마다 점수가 줄어 보이는 문제. 제출 시에는 기획서대로 더한다 |
| 메타에 `special_bonus_score` 추가 | 기획서 §11.1 공식에는 특수 구간 배율이 없어서, 정직한 플레이가 서버 재계산에서 5% 밖으로 밀려 감점됐다. 보너스를 따로 보내고 서버가 구간당 800점으로 clamp |
| 결과 모달에 게임별 상세 기록(거리·콤보·니어미스·아이템·페이즈) 표시 | §12 결과 화면 |
| **`energy.flapDrain` 14 → 10** | §16-6 봇 시뮬레이션 200판 중앙값이 14일 때 **39초**로 목표(60~90초)에 크게 못 미쳤다. 10이면 55초, 8이면 69초. 봇이 아이템을 적극적으로 줍지 않는 '보통 실력' 기준이라 10을 골랐고, 행사 전 실제 플레이로 재튜닝할 것 |

## 5-2. 아울러닝 비주얼 리워크 (벽 · 속도감 · 외부 에셋)

| 결정 | 이유 |
|---|---|
| 장애물을 **게임 모티브**로 다시 그림 — 파이프 벽(플래피/마리오) · 전격 자퍼(젯팩 조이라이드) · 벽돌 블록과 ? 블록(마리오) · 톱니 테두리(지오메트리 대시) · 순찰 드론 | "무엇에 부딪히는지"가 한눈에 읽혀야 부스에서 설명 없이 플레이할 수 있다 |
| 속도감 레이어 추가: 4단 패럴랙스 · 스피드 라인 · 부엉이 잔상 · 바람 먼지 · 터보 줌/링 · 속도 비네트 · km/h 표시 | 같은 속도라도 체감이 완전히 달라진다. 전부 `engine/fx.ts`(렌더 전용)라 게임 로직·점수에는 영향 없음 |
| 외부 **CC0 에셋** 사용 (Kenney 파티클·프로토타입 텍스처·라이트 마스크) | 손으로 그린 도형만으로는 벽면 질감·빛이 한계. 전부 그레이스케일이라 팔레트에 맞춰 틴팅해서 쓴다 |
| HUD 폰트 **Orbitron**(OFL)을 번들 | 숫자 가독성 + SF 톤. 한글은 Pretendard 유지 (Orbitron은 영문·숫자 전용) |
| 에셋 로딩 실패·지연 시 **항상 도형 폴백** | 부스 네트워크가 느려도 게임이 멈추면 안 된다 |
| 도시 스카이라인은 계속 **절차적 생성** | CC0로 쓸 만한 야간 도시 패럴랙스 레이어를 찾지 못했다 (조사 기록: `public/assets/CREDITS.md`) |

## 5-3. 아울 에너지 (스태미나)

포인트만 보고 무한 반복하는 걸 막기 위해 도입. 계산은 전부 서버에서 한다.

| 결정 | 이유 |
|---|---|
| 게임 시작(`start_game_session`)에서 1개 차감 | 제출 시점에 차감하면 세션만 만들고 버리는 식으로 우회할 수 있다 |
| 충전은 **lazy regen** — `owl_energy_at` 기준 시각을 소비한 간격만큼만 앞으로 당긴다 | `now()`로 덮으면 진행 중이던 10분이 사라져 유저가 손해를 본다. 크론도 필요 없다 |
| 자동 충전 상한 10, **부스 지급은 20까지** 허용 | 가득 찬 사람에게 미션 보상을 못 주면 부스 운영이 막힌다 (모바일 게임의 regen cap / hard cap 패턴) |
| 게임 내 드롭은 **아울러닝 P3(900m) 이상**에서만, 하루 5개까지 | "높은 스테이지에서 가끔 등장" 요구. 실력 보상이면서, 에너지를 써서 에너지를 버는 구조라 자기 제한적이다 |
| 드롭은 클라가 주운 사실 + **서버가 스테이지·거리·일일 상한 재검증** | 클라 신고만 믿으면 조작 가능. 조건을 서버 메타 검증과 묶었다 |
| 지급 이력을 `energy_grants`에 남김 | 부원이 누구에게 왜 줬는지 추적 (행사 운영 사고 대비) |
| 데모 모드에서도 에너지를 소모하게 함 | 실제 흐름(부족 → 대기 → 충전)을 Supabase 없이 그대로 확인할 수 있다 |

## 5-4. 공통 15단계(STAGE) 체계 · 실시간 등수

"무한 점수 + 15단계, 단계마다 곱으로 어려워진다"는 요구를 다섯 게임에 같은 규칙으로 깔았다 (`lib/stages.ts`).

| 결정 | 이유 |
|---|---|
| 단계 배율 `1.16^(n-1)` (1→15단계에서 **8.1배**) | 덧셈 증가는 후반에 심심하고, 1.2^14(=12.8배)는 10단계 이후 벽이 된다. 봇 시뮬레이션에서 15단계가 "가능하지만 오래 못 버틴다"에 걸린 값 |
| 15단계를 넘으면 **배율 고정**(무한 스테이지) | 점수는 계속 쌓이되 난이도는 멈춘다 — 상위권이 운으로 갈리지 않게 |
| 점수 보너스는 난이도의 **제곱근**(`stageScoreMult`) | 난이도만큼 점수를 주면 후반 1분이 앞 9분을 덮어써서 초반 플레이가 무의미해진다 |
| 각 게임은 자기 진행도를 0~1 비율로 바꿔 넘긴다 (아울러닝=거리, 서바이버즈·타이머 게임=시간, 로직=해결 수/24) | 게임마다 "진행"의 단위가 달라도 HUD·결과 화면의 `STAGE n/15`는 같은 의미가 된다 |
| 서바이버즈는 단계 배율을 **적 HP·속도**에 곱하되 **보스는 예외** | 보스 HP는 15초 창에 맞춰 잡은 값이라 배율까지 곱하면 아무도 못 잡는다 |
| 로직은 단계 배율을 **점수에 반영하지 않는다** | 서버가 `combo_mult_avg`·`tier_mult_avg`(상한 2.4)로 재계산하므로, 클라가 단계 배율을 더 곱하면 전부 `raw_adjusted` 된다. 로직의 난이도 곱셈은 이미 티어 사다리(1.0→2.4)가 맡는다 |
| 등수는 `fetchMyPosition()`으로 **시작 직전·제출 직후 두 번** 읽어 결과 화면에 `14위 → 11위 ▲3`으로 보여준다 | "실시간으로 등수가 어떻게 떨어졌는지" 요구. 랭킹 뷰를 그대로 쓰므로 서버에 새 API가 필요 없다 |
| 타이퍼·피싱은 **경과시간 선형 증가를 단계 곡선으로 교체**했다 (`stageCurve(stage, 0.4~0.5)`) | 단계 라벨만 붙이고 난이도가 그대로면 "단계마다 곱으로 어려워진다"가 거짓말이 된다. 지수로 눌러 써서 최종 난이도는 기존과 비슷하게 유지했다 (타이퍼 낙하속도 46→130, 스폰 1.7→0.67초) |
| 피싱에 **카드 제한시간**(STAGE 1에서 10초 → STAGE 15에서 4.4초)을 넣었다 | 피싱에는 시간 말고 난이도를 올릴 축이 없었다. 초과하면 오답 처리(−5초)하고 해설을 보여 준다 |
| 로비·랭킹은 `LiveRefresh`(30초·20초 + 탭 복귀 시)로 갱신, `localStorage`에 직전 등수를 남겨 변동 칩을 띄운다 | Realtime 구독을 화면마다 붙이면 부스 와이파이에서 연결 수가 폭증한다. 새로고침 한 번이 더 싸고 안전하다 |

## 5-5. 아울 로직 (OWL LOGIC) — `OWLLOGIC_GDD.md` 구현

| 결정 | 이유 |
|---|---|
| **배선 고정 · 게이트만 꽂기** (GDD안 그대로) | 드래그로 선을 잇게 하면 모바일에서 조작이 무너진다. 탭-투-플레이스면 한 손으로 된다 |
| 회로는 **SVG**, 진리표·부품 트레이는 DOM | 선·노드·신호 흐름은 SVG가 압도적으로 쉽고, 한글 설명은 DOM이 읽기 좋다 (피싱 헌터와 같은 이유) |
| 진리표 줄을 누르면 그 입력 조합이 회로에 들어간다 | 어디가 틀렸는지 눈으로 따라갈 수 있어야 비전공자도 푼다. 입력 핀도 눌러서 0/1을 뒤집을 수 있다 |
| 신호는 색(앰버) + **선 모양**(실선/점선) + 0·1 라벨 3중으로 표시 | 색각 이상 대비 (§12) |
| 꽂힌 게이트가 **쓰지 않는 배선은 끊긴 선**으로 그린다 | 티어 안에서 배선 수를 똑같이 깔아 "3선 칸 = MUX 자리"를 감췄기 때문에, NOT을 꽂으면 두 번째 선이 놀게 된다 |
| 티어 안에서 슬롯 배선 수를 **항상 같게** 깐다 | 배선 수가 다르면 문제를 풀기 전에 정답 게이트가 들통난다 |
| T1~T3은 티어의 모든 부품을 주고, T4·T5만 **딱 맞게** 준다 | 부품이 남아돌면 고민이 없고, 처음부터 모자라면 진입 장벽이 된다 |
| 생성기는 **모든 입력이 결과에 영향을 주는** 진리표만 낸다 | 없으면 T3~T5의 ⅓이 "C는 아무 상관 없는" 문제가 되어 김이 샌다 |
| 최근 진리표 중복 차단 깊이 = 1 / 3 / 10(T3+) | 2입력 함수는 비자명한 게 10개뿐이라 T1·T2에서 10개 차단은 불가능하다 |
| `hint`는 성질만 말하고 **게이트 이름을 절대 넣지 않는다** (1만 문제 검사) | 힌트가 정답 공개면 -8초를 낼 이유가 없다 |
| 자동 제출(진리표가 맞는 순간 정답 처리) | 제출 버튼을 누르는 손맛보다, 맞춘 순간 바로 다음 문제로 넘어가는 리듬이 70초짜리 게임에 맞다 |
| "넘기기"를 `onWrongSubmit`(-3초)으로 재사용 | 자동 제출이라 오답 제출이 없는데, 막혔을 때 빠져나갈 길은 필요하다. 콤보는 유지된다 |
| HUD 점수는 **남은 시간 보너스를 뺀 값** | `raw`에 `남은시간×10`이 들어 있어서 그대로 쓰면 시계가 줄 때마다 점수가 깎여 보인다 |
| 세션을 180초에서 **강제 종료**(`maxSessionSec - 5`) | 해결 보상으로 시간이 계속 늘어나 서버 상한(185초)을 넘으면 그 판이 통째로 `rejected` 된다 |
| 🦉 에너지 드롭 조건 = `tier_max ≥ 4` + 30% | 서버 조건과 클라 조건을 같은 값으로 맞춰 둔다 (`CFG.owlEnergy`) |

## 5-6. 아울 서바이버즈 (OWL SURVIVORS) — `OWLSURVIVORS_GDD.md` 구현

| 결정 | 이유 |
|---|---|
| 적·탄·경험치·파티클은 **SoA 타입배열 풀 + 공간 해시** (런 중 `new` 금지) | 동시 180마리 × 60fps. 객체 배열이면 GC가 튀어 부스 구형 폰에서 끊긴다 |
| 세로 화면에서 캔버스를 **cover(꽉 채우고 자르기)** 로 맞춘다 | contain이면 16:9 놀이터가 폰 화면 가운데 띠만 남는다. 카메라가 따라다니고 월드에 경계가 없어 잘려도 문제가 없다 (항상 960×540 안쪽만 보이므로 스폰 링 620px 규칙도 유지) |
| **보스 HP 6000 → 1500**, 단계 배율 면제 | 15초 창 × 최대 빌드 DPS로는 6000·2600·1800 모두 봇 16판 전부 실패였다. 1500에서 2/16이 잡는다 — 마지막 구간이 "도망치기"가 아니라 승부가 된다 |
| **환산 계수 K 20 → 30** (서바이버즈만) | K=20이면 스테이지 1 풀클리어(raw ≈ 6,300)만으로 상한 300P에 닿아 난이도 선택이 무의미해진다 |
| 트로이목마 스폰 가중치를 25/18/20 → 14/10/10 | 분열 3마리가 스폰 예산 밖이라 초당 7.7킬까지 올라가 서버 거부선(8킬/초)에 붙었다 |
| 💾 램 패시브는 `ceil(lv/3)` | `floor`면 Lv1·Lv2 카드가 아무 효과가 없는 꽝 카드가 된다 |
| 적 속도·반경·접촉 피해는 기획서가 "느림/보통/빠름"만 정해 두어 여기서 확정 | 표는 `games/survive/engine/enemies.ts`의 `ENEMY_SPEC` 한 곳 |
| 레벨업 카드는 **게임을 멈추고** 3장, 리롤 1회·건너뛰기(XP 10%) | 폰에서 움직이면서 고르게 하면 그냥 아무거나 누르게 된다 |
| 저사양(45fps 2초 연속) 감지 시 파티클·적 상한을 낮춘다 | 부스에 어떤 폰이 올지 모른다 |

## 5-7. 운영(관리자) 시스템 · 타이퍼·피싱 서버 검증

| 결정 | 이유 |
|---|---|
| 타이퍼·피싱은 **재계산이 아니라 상한 클램프** | 어떤 단어를 어떤 콤보로 쳤는지 서버는 모른다. 대신 메타로 만들 수 있는 최대 점수(타이퍼 = 파괴 수 × 440, 피싱 = `phish_max_score(정답, 최고연속)`)를 구해 그 위로만 깎는다 |
| 메타 키가 **아예 없으면 검사도 클램프도 건너뛴다** | 예전 클라이언트가 캐시에 남아 있으면 정상 플레이가 통째로 0P가 된다. 메타가 없으면 예전 동작(=300P 상한만)으로 돌아갈 뿐이라 손해가 없다 |
| 검증의 축은 **서버 시계** | 타이퍼: 블록은 0.6초에 하나씩만 내려오니 단어 수가 경과시간을 못 넘는다. 피싱: 오답 1회가 제한시간을 5초 깎으니 `경과 ≤ 90 − 5 × 오답 + 8`이어야 한다. 클라이언트끼리 아귀가 맞는지가 아니라, 서버가 잰 시간과 맞는지를 본다 |
| 방화벽 게이지는 **정확히** `min(100, 놓친 수 × 20)` 이어야 한다 | 클라이언트가 그렇게만 올린다. 어긋나면 메타를 손댄 것이다 |
| 감사 로그는 RPC를 고치지 않고 **테이블 트리거**로 모은다 | `submit_game_session` 같은 600줄짜리 함수를 다시 만들 이유가 없다. 어떤 경로로 바뀌든(관리자 RPC·부스 RPC·수기 SQL) 똑같이 남는다 |
| "본인이 본인 행을 바꾼 것"은 감사 로그에 남기지 않는다 | 안 그러면 게임 제출마다 포인트·에너지 변경이 쌓여서 운영 기록이 게임 로그가 된다 |
| 마스터 관리자 학번은 **관리자가 0명일 때만** 승격시킨다(`bootstrap_only`) | 공개 레포라 학번이 노출돼도, 첫 관리자가 생긴 뒤에는 효력이 없다. SQL 편집기 없이 첫 관리자를 만들 수 있는 게 목적이다 |
| 설정 변경은 **키 화이트리스트 + 서버 검증**(`admin_set_config`) | `app_config` 는 admin RLS 로 직접 update 도 되지만 그 경로엔 검증이 없다. K값에서 게임 키 하나만 빠져도 그 게임 제출이 통째로 실패한다 |
| 아울 에너지 설정은 보낸 키만 **덮어쓰기(merge)** | 화면에서 충전 간격만 바꿨는데 드롭 조건이 통째로 날아가면 안 된다 |
| 대시보드는 RPC 한 번(`admin_stats`)으로 끝낸다 | 부스 노트북에서 30초마다 갱신한다. 테이블 여러 개를 따로 조회하면 RLS·왕복 비용이 커진다 |

## 5-8. 아울 서바이버즈 v2 (전면 재설계)

`OWLSURVIVORS_GDD_v2.md`로 v1(구역 3개를 한 판에 도는 방식)을 폐기하고 다시 만들었다.
**한 스테이지 = 한 런**, 보스를 잡아야 다음 스테이지가 열린다.

| 결정 | 이유 |
|---|---|
| **아레나(1600×900) 경계를 새로 뒀다** | §8의 "장애물은 맵 면적의 6~9%"는 맵에 넓이가 있어야 계산된다. v1의 무한 평면에서는 밀도라는 개념 자체가 성립하지 않는다. 카메라는 플레이어를 따라가되 경계 안으로 제한된다 |
| 장애물을 **격자에 놓는다** (홀수 행은 반 칸 밀어서) | 무작위로 뿌리면 서로 막아서 밀도가 3%를 못 넘겼다(실제로 그렇게 만들어 보고 알았다). 행·열 간격이 이미 통로 폭을 보장하는 격자면 6~7%가 안정적으로 나온다 |
| 통로 판정은 **축 하나만** 140px 이상이면 통과 | 대각선 거리로 재면 실제로는 뚫려 있는데도 배치를 거부한다. 두 사각형 사이는 x나 y 중 한 축이 열려 있으면 지나갈 수 있다 |
| 밀도 목표를 6~7%로 잡는다 (상한은 9% 유지) | 9%까지 채우려면 큰 장애물만 깔아야 해서 맵이 단조로워진다 |
| **적이 주는 XP도 스테이지마다 +14%** | 기획서는 체력(+18%)·공격력(+12%)만 올린다. 그런데 시작 장비는 늘 같아서 S7이면 벌레 한 마리에 2발이 필요해 초반에 길이 막힌다. 성장 속도도 같이 올려야 "더 세진 적"이 "못 이기는 적"이 되지 않는다 |
| **시작 보정** — 3스테이지마다 시작 스킬 Lv+1, 5스테이지마다 시작 스킬 1개 추가, 스테이지당 최대 체력 +8 | 위와 같은 이유. "이전 스테이지를 클리어하고 온 부엉이"라는 설정으로 납득시킨다. 봇 시뮬 기준 S7 생존이 27초 → 56초로 늘었다 |
| 스폰 예산을 **√체력배율에 반비례**하게 줄인다 | 높은 스테이지는 "더 세지되 더 적게". 체력 2배인데 수까지 그대로면 처치 속도가 스폰 속도를 절대 못 따라간다 |
| 보스 **접촉** 피해 25 → 18 | 25는 1스테이지 첫 판에서 네 번만 닿아도 죽는다. 몸통 박치기 대신 패턴(장판·돌진)으로 압박한다. 이 한 줄로 봇 클리어율이 2/8 → 5/10이 됐다 |
| 스킬 20종을 **20개 함수가 아니라 유형(archetype) 핸들러**로 | 추적·빔·오라·궤도·연쇄처럼 동작이 겹친다. 진화는 "같은 유형 + 강화 플래그"라 12종을 따로 안 짜도 된다 |
| 보스 15종도 **패턴 12종의 조합**으로 | 보스마다 클래스를 만들면 15개를 다 검증해야 한다. 데이터로 조합하면 패턴 하나만 고쳐도 전부 고쳐진다 (`data/stages.ts`의 `patterns`) |
| 환산 계수 K 30 → **20** (기획서 §11.2) | v1은 한 판에 구역 3개를 돌아 원점수가 컸다. v2는 한 스테이지만 돌아서 작아지므로 기획서 값으로 되돌렸다 |
| **§16의 "게임 4종으로 축소"는 따르지 않았다** | 기획서는 타이퍼·아울러닝을 빼고 4종으로 가자고 제안하지만, 이미 5종 유지로 결정돼 있다. 라인업만 그대로 두고 나머지 스펙 변경(시간 제한·game_guards·profiles.meta)은 전부 반영했다 |
| `profiles.meta.survive_unlock` → **`survive_stage`** (의미도 "고를 수 있는 단계" → "클리어한 최고 단계") | 무한 스테이지에는 상한이 없어서 "해금 단계" 개념이 맞지 않는다. 마이그레이션이 기존 값을 -1 해서 옮긴다 |
| 거부 기준을 코드가 아니라 **`app_config.game_guards`** 에 둔다 (§16) | 행사 중에 초당 처치 상한 같은 걸 배포 없이 조정할 수 있어야 한다 |
| 다크는 발광, 라이트는 **외곽선 + 그림자** (`theme.ts`의 `neon()`/`outline()`) | 밝은 배경에서 glow는 그냥 안 보인다. 같은 코드로 두 테마를 그리면 라이트가 죽는다 |

## 5-9. 아울스페이스 (OWL SPACE) — `OWLSPACE_GDD.md` 구현

세로 탄막 슈팅. 서바이버즈와 **의도적으로 정반대**로 설계된 게임이라 엔진을 공유하지 않는다.

| 결정 | 이유 |
|---|---|
| 보스 패턴을 **DSL 데이터**(`data/patterns.ts`)로 선언하고 실행기(`engine/emitter.ts`)가 읽는다 | 기획서 §13의 요구. 보스 15종 × 페이즈를 코드로 쓰면 아무도 못 고친다. 테스트가 "모든 보스 페이즈가 데이터에 있다"를 검증한다 (§14-6) |
| 각도 규약은 **0도 = 화면 아래쪽**, 시계 방향 + | 세로 슈팅은 탄이 아래로 가는 게 기본이라, 수학 표준(0도=오른쪽)보다 데이터 읽기가 쉽다 |
| `repeat`(연사)는 **볼리 큐**로 처리 | 회전 나선(`rotateDeg`)을 구현하려면 "발사 예약"이 필요하다. 적이 죽으면 큐도 같이 정리된다 |
| 충돌은 **거리 제곱 비교만** (공간 해시 없음) | 판정점 1개 vs 탄 900발은 단순 순회가 더 빠르다 (§13). 그리드는 오히려 오버헤드 |
| 그레이즈는 탄마다 `grazed` 플래그로 **1회만** | 같은 탄이 여러 프레임 스치면 점수가 폭주한다 (§14-7) |
| 라이트 테마 적 탄 `#DC2626` → **`#B91C1C`** | 기획서 색이지만 배경(#F4F6FB) 대비가 4.47:1 로 §14-11(4.5:1)에 **미달**한다. 한 단계 진하게 바꿔 5.9:1 로 맞췄다. 테스트가 WCAG 대비를 계산해 지킨다 |
| "내 탄 vs 적 탄"은 명도 대비가 아니라 **채널 거리**로 검사 | 파랑(#0E7490)과 빨강(#B91C1C)은 명도가 비슷해 대비비로는 1.1 이 나온다. 색이 다른지는 RGB 거리로 봐야 한다 |
| 장애물 접촉은 **넉백만** | 탄막 게임에서 지형 즉사는 불합리하다 (§7). 피격은 오직 적 탄과 자폭 적 |
| 보스 등장 55초 · 하드캡 180초 | 기획서 §1 의 "1판 70~150초"에 맞춘 값. 서버 `min_clear_sec`(55)과 같은 값을 쓴다 |
| 스킬은 30종으로 **단순하게** | 기획서 §5 의 의도 그대로. 탄막은 화면을 읽는 데 인지 자원을 써야 해서, 빌드까지 복잡하면 둘 다 망한다 |
| §15 의 "라인업 축소" 권고는 따르지 않았다 | 5종 유지 결정이 이미 있었고 여기에 스페이스가 더해져 **6종**이 됐다. 대신 스펙 변경(enum·game_k·game_limits·game_guards·profiles.meta)은 전부 반영했다 |

## 5-10. 리퀴드 글래스 리디자인 (§13 개정)

레퍼런스(Liquid Glass UI Kit)에 맞춰 플랫폼 전체의 톤을 바꿨다. **토큰 이름은 그대로 두고 값만 바꿨기 때문에**
기존 화면들은 코드를 고치지 않아도 새 톤을 따라간다.

| 결정 | 이유 |
|---|---|
| 강조색을 앰버(#FFB020) → **바이올렛 `--color-neon: #a78bfa`** 로 교체. 앰버는 `--color-amber` 로 분리해 **부엉이·아울 에너지·뽑기 티켓에만** 쓴다 | 레퍼런스의 주 색이 시안→바이올렛→마젠타 그라데이션이다. 토큰 이름(`neon`)을 바꾸면 40여 개 파일을 다 고쳐야 해서 **의미(강조색)는 유지하고 값만** 바꿨다. 앰버를 브랜드 전용으로 남겨 부엉이 정체성은 지킨다 |
| 배경 `night` `#0B1020` → **`#070b18`**, 패널도 한 단계씩 어둡게 | 유리판(반투명 흰색)이 떠 보이려면 뒤가 충분히 어두워야 한다 |
| 카드는 `.card` 하나로 통일 — 흰색 7%→2% 그라데이션 + 1px 헤어라인 + `backdrop-filter: blur(18px) saturate(1.3)` + 안쪽 하이라이트/그림자 | 레퍼런스의 유리판을 CSS만으로 재현. 이미지 에셋 없이(§13) 가능하다 |
| 그라데이션 테두리는 `.grad-line`(`mask-composite: exclude`) 로 구현 | `border-image` 는 `border-radius` 와 같이 못 쓴다. 마스크로 1px 링만 남기는 방식이 둥근 모서리에서 유일하게 깨끗하다 |
| 기본 버튼(`primary`)은 **시안→바이올렛→마젠타 채움 + 바깥 글로우**, 보조(`outline`)는 유리 + 그라데이션 헤어라인 | 레퍼런스의 Primary/Secondary 대응. 텍스트는 `text-night`(어두운 글자)로 대비를 확보 |
| `.scanlines`(CRT 주사선) → **필름 그레인**(`mix-blend-mode: overlay`) | 유리 질감엔 주사선보다 미세 노이즈가 맞다. 클래스 이름은 `layout.tsx`가 쓰고 있어 그대로 뒀다 |
| 배경은 색 블룸 3개 + 아주 옅은 56px 격자 + 90초 회전 광선. 광선은 `blur(40px)` + 방사형 마스크로 경계를 지운다 | 마스크 없이 `conic-gradient`만 쓰면 빈 화면(관리자 대시보드)에서 부채꼴 경계가 그대로 보인다. `prefers-reduced-motion`에서는 회전을 멈춘다 |
| 게임 캔버스 팔레트(`games/core/canvas.ts`의 `COLORS`)와 서바이버즈·스페이스 다크 테마도 같은 값으로 맞췄다 | 캔버스는 CSS 변수를 못 읽어서 값을 복제해 둔 것이다 — 한쪽만 바꾸면 로비와 게임의 톤이 어긋난다 |
| 게임 안의 **플레이어(부엉이 기체)는 계속 앰버** | 적(바이올렛)·내 탄(시안)·적 탄(핑크)과 색이 겹치지 않고, 브랜드색이 "나"를 가리키는 게 읽기 쉽다 (§14-11) |
| 로비 게임 카드는 `sm:` 이상에서 2열 | 6종이 되면서 세로로 너무 길어졌다. 모바일(375px)에서는 1열 그대로 |

## 5-11. 설정 — 테마 · 화면 크기 · 소리 · 언어

행사장은 밝고, 오는 사람의 기기와 시력과 언어가 제각각이다. 그래서 **로그인 없이도 열리는**
`/settings` 한 곳에서 화면·소리·언어를 바꾼다.

| 결정 | 이유 |
|---|---|
| 테마·화면 크기·소리는 **localStorage**, 언어만 **쿠키** | 언어는 서버 컴포넌트가 렌더할 때 알아야 해서 쿠키여야 한다. 나머지는 서버가 알 필요가 없다 |
| 테마·화면 크기는 `<head>`의 **인라인 스크립트**가 첫 페인트 전에 적용한다 | 나중에 적용하면 어두운 화면이 번쩍이거나 글자 크기가 한 번 튄다 |
| 화면 크기는 `html { font-size: calc(100% * var(--ui-scale)) }` 로 4단계(90·100·112·125%) | Tailwind 가 rem 을 쓰기 때문에 글자와 여백·버튼이 **같이** 커진다. 게임 캔버스는 px 고정이라 영향이 없다 |
| 효과음은 **오디오 파일 없이 WebAudio 로 합성**한다 (`lib/sound.ts`) | 플랫폼 UI를 이미지 없이 그리는 것과 같은 이유. 사각파 + 짧은 엔벌로프라 8비트 느낌이 나고 용량이 0이다 |
| 클릭음은 버튼마다 달지 않고 `SoundBoot` 가 문서 전체에서 한 번만 듣는다 | `components/ui/Button.tsx` 는 서버 컴포넌트에서도 쓰여서 onClick 을 달 수 없다. capture 단계로 들어 이벤트를 막는 컴포넌트가 있어도 소리가 난다 |
| i18n 은 **next-intl**, 단 `/en/...` 같은 **경로는 쓰지 않는다**(`localePrefix` 없이 쿠키만) | App Router 의 사실상 표준이고 서버·클라이언트 양쪽을 한 API로 덮는다. 다만 이 앱은 로그인 뒤에 쓰는 부스용이라 SEO 가 필요 없고, 경로를 나누면 미들웨어(role·verified·운영시간)까지 갈라진다 |
| 번역 대상은 **플레이어가 보는 화면 전부**(랜딩·로비·랭킹·티켓·내 기록·설정·가입/로그인·인증 대기·게임 인트로/결과 + 랭크 이름 17종). 관리자·부스 키오스크·전광판은 한국어 | 그쪽은 부원이 쓰는 운영 화면이다. 행사 당일 손이 빨라야 하는 쪽을 굳이 두 언어로 만들 이유가 없다 |
| 게임 **안의 HUD·스킬 카드 문구는 아직 한국어** | 게임마다 수백 줄이라 이번 범위에서 잘랐다. 필요하면 `messages/` 에 게임별 네임스페이스를 더하면 된다 |
| 서버 액션(가입·로그인) 오류 문구도 `getTranslations` 로 번역한다 | 폼 오류만 한국어로 튀어나오면 번역 모드가 깨져 보인다 |
| 라이트 테마에서 `*-soft` 토큰은 **더 진한** 색이 된다 | `soft` 는 "배경 위에서 읽히는 쪽"이라는 뜻으로 쓰고 있다. 이름을 바꾸면 40여 개 파일을 고쳐야 한다 |

## 5-12. 스페이스 네온 (리퀴드 글래스 2차 보정)

레퍼런스(스페이스 그라데이션 팔레트 · 유리 로그인 카드 · 네온 카드 차트)에 맞춰 §5-10 위에 한 겹 더 얹었다.

| 결정 | 이유 |
|---|---|
| 배경에 **별밭**(`.night-sky::before`)과 **대각선 성간 광선**(`::after`)을 깔았다. 격자·회전 광선은 뺐다 | 레퍼런스는 전부 "우주"다. 격자는 대시보드처럼 보이고, 회전 광선은 빈 화면에서 부채꼴 경계가 보였다 |
| 별은 크기가 다른 두 겹을 **다른 주기(440px·660px)로 타일링**한다 | 별 하나하나를 좌표로 박으면 CSS가 수십 줄이 된다. 주기가 다른 두 겹이면 반복이 눈에 안 띈다 |
| 라이트에서는 `--star-opacity: 0`으로 별을 끄고 `.sky-day::after`의 **따뜻한 햇살 줄기**만 남긴다 | 낮 하늘에 별이 보이면 이상하다 |
| **`card-neon`** 유틸 추가 — 파랑→바이올렛 채움 + 밝은 테두리 + 바깥 발광 | 레퍼런스의 카드가 이 모양이다. 다만 **게임 카드·랭크 히어로·획득 포인트처럼 눈이 먼저 가야 하는 타일에만** 쓴다. 전부 여기에 담으면 화면이 소란스러워진다 |
| **`display`** 유틸(900 웨이트 · 자간 −0.035em) 추가 | 레퍼런스의 큰 제목 톤. 제목마다 `font-black tracking-tight`를 반복하지 않으려고 유틸로 뽑았다 |
| 입력칸은 **`input-glass`**(테두리만 또렷한 유리) | 레퍼런스 로그인 폼이 이 방식이다. 배경을 채우는 대신 테두리 대비로 읽힌다 |
| 로비의 "플레이"는 전부 **그라데이션 버튼 하나로** 통일 | 네온 카드 위에서는 외곽선 버튼이 묻힌다 |

## 6. 남은 이슈 / 튜닝 포인트

- **로직의 "최적화" 보너스(+80·+2초)는 지금 사실상 상시 보너스**다. 생성기가 `minGates = 슬롯 수`인 문제만 내서
  풀면 항상 최적이기 때문. 차별화하려면 생성기가 가끔 `minGates = 슬롯 수 - 1`인 문제를 내야 한다
  (엔진·판정은 이미 그 경우를 지원하고 테스트도 있다).
- 서바이버즈 보스는 스테이지 2·3에서는 체력 배율(×1.3·×1.7) 때문에 사실상 못 잡는다. 대신 점수 배율로 보상한다.
- 서바이버즈 v2는 **가로 전용**이 됐다(§2). 세로로 들면 회전 안내가 뜨고 게임이 멈춘다 — v1의 cover 방식은 폐기.
- 서바이버즈 봇 시뮬 기준 1스테이지 클리어율은 **8판 중 2~5판**이다. 기획서 §15-10의 "70%"는 사람 기준이고,
  봇은 '가장 가까운 적 반대로 도망만 치는' 수준이라 그대로 비교할 수 없다. 행사 전에 사람으로 한 번 재 볼 것.
- 서바이버즈 7스테이지는 봇이 보스(100초)까지 못 간다. 사람 기준 30~45%(§15-10)인지는 확인이 필요하다.
- 아울스페이스 봇 시뮬은 1스테이지를 6/6 클리어한다(§14-9 목표 75%). 다만 봇은 "가장 가까운 탄에서
  멀어지기"만 하는 수준이라 **그레이즈를 거의 하지 않는다** — 사람 점수 분포는 행사 전에 다시 재야 한다.
- 아울스페이스 8스테이지는 봇이 보스(55초)에서 죽는다. §14-9 의 30~45%는 사람 기준이다.
- ~~게임이 6종이 되면서 로비 카드가 길어졌다~~ → 리디자인에서 `sm:` 이상 2열로 바꿨다(§5-10). 모바일은 여전히 1열이라 스크롤이 길다.
- 스킬 50종 중 **로그 폭탄(A09)의 킬 카운터는 `WeakMap`에 들고 있다**. 슬롯 객체를 새로 만들면 카운터가
  초기화되니, 카드 적용 로직을 바꿀 때 주의할 것.
- ~~타이퍼·피싱은 서버가 원점수를 재계산하지 않는다~~ → 20260927 마이그레이션에서 메타 검증 + 상한
  클램프를 넣었다 (§5-7). 다만 메타를 아예 안 보내면 예전처럼 300P 상한만 걸린다.
- **마스터 관리자 기본 학번 `999999999` 는 그대로 두지 말 것.** 첫 관리자가 생기기 전까지는 누구나
  그 학번으로 가입해 관리자가 될 수 있다. 행사 전에 `/admin → 설정`에서 본인 학번으로 바꾼다.
- **비행 게임 최소 시간 3초**: 일부러 바로 죽어도 30P가 들어온다(3초당 30P). 행사 중 악용이 보이면
  `app_config.game_limits.flight.min_sec`을 올리거나, 최소 포인트를 경과시간에 비례하게 바꿀 것.
- 피싱 헌터에서 일부러 오답만 내면 20초 하한(`phish.min_sec`)보다 빨리 끝날 수 있다 → 그 판은 `rejected`(0P).
- 추첨 시뮬레이션(`supabase/tests/gacha_simulation.sql`)의 ±0.5%p는 10만 회 기준 약 3σ라 정상 구현도 1% 확률로 실패한다.
- 아울 에너지 충전 속도(10분/개)는 행사 당일 대기열 길이를 보고 `app_config.owl_energy.regen_min`으로 조정할 것.
- 부스 위치·상품 재고·K값은 `app_config`/`prizes`에 placeholder로 들어가 있다. 행사 전에 채울 것 (§15).
- `delete from auth.users`(계정 삭제)와 `auth.users` 트리거는 Supabase 기본 권한에 기대므로, 실제 프로젝트에
  `db push` 후 한 번 스모크 테스트할 것.
